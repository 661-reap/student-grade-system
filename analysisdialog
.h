#ifndef Error in " 'analysisdialog
.h '.toUpperCase().replace(/^[1-9]/, '_ ').replace(/[^_a-zA-Z1-9]/g, '_ ')": SyntaxError: Expected token `;'
#define Error in " 'analysisdialog
    .h '.toUpperCase().replace(/^[1-9]/, '_ ').replace(/[^_a-zA-Z1-9]/g, '_ ')": SyntaxError: Expected token `;'

#include <QDialog>

    namespace Ui
{
    class AnalysisDialog;
}

class AnalysisDialog : public QDialog
{
    Q_OBJECT

public:
    explicit AnalysisDialog(QWidget *parent = nullptr);
    ~AnalysisDialog();

private:
    Ui::AnalysisDialog *ui;
};

#endif // Error in " 'analysisdialog
.h '.toUpperCase().replace(/^[1-9]/, '_ ').replace(/[^_a-zA-Z1-9]/g, '_ ')": SyntaxError: Expected token `;'
